export function Footer() {
  return (
    <footer className="py-6 text-center flex flex-col items-center justify-center gap-1">
      <p className="text-[10px] sm:text-xs text-white/50 tracking-[0.2em] uppercase font-semibold">
        DESIGN & INTERFACE BY
      </p>
      <p className="text-[10px] sm:text-xs text-white tracking-[0.2em] uppercase font-bold">
        OXIGEN
      </p>
    </footer>
  );
}
